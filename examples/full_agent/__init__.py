"""examples.full_agent — Reference implementations using teragent library APIs

These modules demonstrate how to combine library primitives with specific personas
and EventBus orchestration to build a complete Design→Plan→Review→Execute pipeline.

Key design principle: Library provides mechanism (retry, extraction, checks),
reference implementations provide persona (system prompts, event flow).
"""
